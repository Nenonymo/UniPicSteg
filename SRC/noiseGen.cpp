#include "noiseGen.h"

using namespace std;

void noise(unsigned long int size, uint16_t* array, uint8_t* refChannels, uint8_t alpha)
{
    for (unsigned long int pixel = 0; pixel<size; pixel++)
    {
        
    }
}